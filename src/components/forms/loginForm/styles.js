import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btnContainer: {
    alignSelf: 'center'
  },
  btnLogin: {
    marginTop: 'auto',
    alignSelf: 'flex-end',
    marginBottom: 30
  },
  inputsContainer: {
    flex: 1
  }
});

export default styles;
